<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">
    <uses-permission android:name="android.permission.INTERNET"/>
    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.PROJECTPFE"
        android:usesCleartextTraffic="true"
        tools:targetApi="31">
        <activity
            android:name=".LoginActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".RegisterActivity" />
        <activity android:name=".PersonalizationQuestionsActivity" />
        <activity android:name=".PersonalizationActivity" />
        <activity android:name=".ProfileActivity" />
        <activity android:name=".TodoActivity" />
        <activity android:name=".TodoAiActivity" />
        <activity android:name=".DoingActivity" />
        <activity android:name=".DoneActivity" />
        <activity android:name=".HomeActivity"/>
        <activity android:name=".DashboardActivity" />
        <activity android:name=".NotificationsActivity"/>
        <activity android:name=".CommunityActivity" />
        <activity android:name=".GroupChatActivity" />


    </application>

</manifest>